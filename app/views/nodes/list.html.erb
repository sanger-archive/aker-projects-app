<%= javascript_tag do %>
  window.currentNodeId = '<%= @node.id %>'
<% end %>

<%= render "show", locals: { tab: "list" } do %>
  <div id="list" class="tab-pane active">
    <br/>
    <div class="col-md-4">
      <%= render 'side_menu' %>
    </div>

    <div class="col-md-8">
      <% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>"><%= value %></div>
      <% end %>

      <% @node.parents.each do |p| %>
        <%= linknode(p) %> /
      <% end %>
      <b><%= @node.name %></b>

      <% if @node.root? %>
        <h2>Sanger Programs</h2>
      <% else %>
        <h2><%= @node.name %></h2>
      <% end %>

      <% if @node.owner_email %>
        <p><b>Owner:</b> <%= @node.owner_email %></p>
      <% end %>

      <% if @node.description? %>
        <p><b>Description: </b> <%= @node.description %></p>
      <% end %>

      <% if @node.cost_code? %>
        <p><b>Cost Code:</b> <%= @node.cost_code %></p>
      <% end %>

      <% if @node.active_children.empty? %>
        No children
      <% else %>
        <ul>
          <% @node.active_children.each do |c| %>
             <li>
                <b><%= linknode(c) %></b>
                <% if check_write_permission_for_node(c) %>
                  (<%= edit_node(c) %> /
                  <%= delete_node(c) %>)
                <% end %>
              </li>
          <% end %>
        </ul>
      <% end %>

      <% if (!@node.root? && check_write_permission_for_node(@node)) %>
        <h4>Add child node</h4>
        <%= bootstrap_form_for(@child, url: nodes_path, method: "post") do |f| %>
          <%= render "node_form", f: f, write_permission: true %>
          <%= f.submit("Create") %>
        <% end %>
      <% end %>

    </div>
  </div>
<% end %>
